#!/bin/rc

for(i in `{ls libc/*.c}){
	stem=`{echo $i |sed 's/\.c$//' |sed 's/libc\///g'}
	echo /usr/glenda/fictools/vbcc/bin/vc -Ilibc/include -Ic '-module-name='$stem -c $i
	/usr/glenda/fictools/vbcc/bin/vc -Ilibc/include -Ic '-module-name='$stem -c $i
}
